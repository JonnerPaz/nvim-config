[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "dotfiles-setup"
version = "0.1.0"
description = "Bootstrap - Entorno de desarrollo"
requires-python = ">=3.10"

[project.scripts]
dotfiles-setup = "install_scripts.cli:main"
